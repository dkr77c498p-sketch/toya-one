'use strict';
const assert=require('node:assert/strict');
const fs=require('node:fs');
const path=require('node:path');
const vm=require('node:vm');
const source=fs.readFileSync(path.join(__dirname,'../docs/attachment-rate-admin.js'),'utf8');
new vm.Script(source);
assert(source.includes('重機アタッチメント使用料（管理者用）'));
assert(source.includes('id="arDaily"'));
assert(source.includes('1台・1日あたり（円）'));
assert(source.includes('1台・1時間あたり（自動計算）'));
assert(source.includes('readonly placeholder="日額÷8時間"'));
assert(source.includes("const dailyToHourly=n=>Math.round(Number(n)/8*10000)/10000"));
assert(source.includes("const hourlyToDaily=n=>Math.round(Number(n)*8*10000)/10000"));
assert(source.includes("if(tools&&tools.nextElementSibling!==card)tools.after(card)"));
assert(!source.includes('この時間単価を保存'));
const dailyToHourly=n=>Math.round(Number(n)/8*10000)/10000;
assert.equal(dailyToHourly(5000),625);
assert.equal(dailyToHourly(30000),3750);
assert.equal(dailyToHourly(5300),662.5);
console.log('Passed attachment daily-rate input, conversion and visible placement checks.');
